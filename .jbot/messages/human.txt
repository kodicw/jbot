CLARIFICATION FROM HUMAN: Regarding "Multi-User Project Isolation" - this simply means we will use Home Manager to create different Linux users to separate entirely different projects (e.g., Project A runs under User A, Project B runs under User B). Do not attempt to split the internal JBot components (like infra vs ops) into multiple users for the same project. Keep the current project architecture cohesive under its current user.